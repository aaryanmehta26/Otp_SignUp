async function sendOTP(target: string) {
    /// we can use Twillio service or any npm package // need to research about that
  }
  
  async function verifyOTP(target: string, otp: string) {
    // Mock function to verify OTP
    return true;
  }
  
  export { sendOTP, verifyOTP };
  